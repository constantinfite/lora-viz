module.exports = {
    //... other settings
    rules: {
        //... other rules
        // use 'off' instead of "warn" to disable the rule and the error message
        'prettier/prettier': ['off', {
            endOfLine: 'auto',
        },
        ],
    },
}